# Optional: override the model used for LLM-based funnel stage classification.
# Defaults to the main chat client's model if not set.
# Use a cheap/fast model (e.g. haiku-class) to reduce classification cost.
# Example: claude-haiku-4-5-20251001
SALES_STAGE_CLASSIFIER_MODEL=
