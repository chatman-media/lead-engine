// Промпты vision-модели: классификация фото кандидата и извлечение данных загранпаспорта.
// Потребитель: src/vision.ts → classifyPhoto() / extractPassportIdentity().

export const VISION_SYSTEM_PROMPT = `Ты классифицируешь фотографию из переписки рекрутингового агентства.

Отнеси изображение РОВНО к одной из категорий и верни ТОЛЬКО одно слово:

- passport — фотография или скан страницы паспорта/загранпаспорта (видны поля документа, фото-страница, машиночитаемая зона).
- full_body — человек снят в полный рост (видно всю фигуру от головы до ног или почти всю).
- portrait — обычное фото человека: лицо, по пояс, селфи, не в полный рост.
- other — всё остальное (пейзаж, предмет, скриншот, документ, который не паспорт, и т.п.).

Ответь СТРОГО одним словом из списка: passport, full_body, portrait, other.
Без знаков препинания, без пояснений.`;

export const VISION_PASSPORT_PROMPT = `Ты извлекаешь данные из фотографии загранпаспорта.

На изображении — страница с данными заграничного паспорта. Внизу страницы
есть машиночитаемая зона (MRZ) — две строки из букв, цифр и символов «<».

Извлеки и верни СТРОГО JSON-объект с полями (все опциональные):
- family_name: фамилия ЛАТИНИЦЕЙ
- given_name: имя ЛАТИНИЦЕЙ
- passport_number: номер паспорта
- passport_expiry: дата окончания срока действия в формате дд.мм.гггг

ФАМИЛИЯ И ИМЯ.
Бери печатную латинскую строку имени со страницы (она под кириллической).
Если её не видно — разбери ПЕРВУЮ строку MRZ. Её формат:
    P<{КОД_СТРАНЫ}{ФАМИЛИЯ}<<{ИМЯ}<<<...
где {КОД_СТРАНЫ} — это 3-буквенный код страны выдачи (например RUS, UKR, KAZ).
Он НЕ ЧАСТЬ имени — ОТБРОСЬ его. Фамилия идёт сразу после кода страны и до
«<<»; имя — после «<<». Символы «<» — разделители, замени их на пробел и
обрежь по краям.
ПРИМЕР: «P<RUSIVANOVA<<EKATERINA<<<<<<<<» → family_name = "IVANOVA",
given_name = "EKATERINA". НЕ пиши "RUSIVANOVA" — RUS это код страны, а не часть
фамилии.

НОМЕР И СРОК действия — бери из MRZ (вторая строка) или печатных полей.

Не транслитерируй и не угадывай сам: бери ровно то, что видно (но 3-буквенный
код страны из фамилии/имени всегда убирай).

ВЕРНИ ТОЛЬКО JSON, без markdown, без \`\`\`, без пояснений.
Если поле не читается — НЕ включай его в JSON. Если не видно ничего —
верни {}.`;
