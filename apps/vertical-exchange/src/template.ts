import type { VerticalTemplate } from "@chatman-media/verticals";
import { EXCHANGE_FUNNEL_STAGES } from "./funnel-stages.ts";
import { EXCHANGE_INTAKE } from "./intake.ts";

/**
 * Exchange vertical. Обменный пункт в Пхукете: крипта (USDT/BTC/ETH),
 * рублёвые переводы на карту и наличные → THB.
 *
 * Воронка: quote_request (intake) → rate_confirmation → kyc_collection →
 * awaiting_payment → qr_delivery → completed | cancelled.
 *
 * Реальный флоу: клиент пишет что меняет → бот фиксирует курс → собирает
 * KYC для крупных сумм → выдаёт реквизиты по способу оплаты (кошелёк или
 * карта) → клиент платит и шлёт пруф → оператор генерирует cardless QR
 * для снятия THB в банкомате и отправляет клиенту.
 *
 * Курс и реквизиты — из KB / настроек тенанта. Бот не называет курс, пока
 * он не задан оператором. QR на старте генерируется вручную оператором.
 */
export const EXCHANGE_V1: VerticalTemplate = {
  slug: "exchange_v1",
  displayName: "Обменный пункт (Пхукет) — v1",
  version: 1,
  funnelStages: EXCHANGE_FUNNEL_STAGES,
  questionnaire: EXCHANGE_INTAKE,
  systemPromptFragment:
    "Ты — AI-ассистент обменного пункта в Пхукете. Меняем крипту (USDT/BTC/ETH), " +
    "переводы в рублях на карту и наличные — всё на тайские баты (THB). " +
    "Задачи: (1) выяснить что отдаёт клиент (актив + способ оплаты) и сумму; " +
    "для USDT обязательно уточни сеть — TRC20, ERC20 или BEP20. " +
    "(2) Подтвердить курс и итоговую сумму THB с учётом комиссии. " +
    "(3) Для крупных сумм собрать имя и фото паспорта. " +
    "(4) Выдать реквизиты для оплаты по выбранному способу: крипта → адрес кошелька " +
    "в нужной сети; рубли → карта/счёт (Сбер/Тинькофф). Затем дождаться пруфа — " +
    "tx hash для крипты или скрин перевода для рублей. " +
    "(5) После подтверждения оператором — сообщить, что готовится QR для снятия THB в банкомате. " +
    "Отвечай кратко, по-деловому, на русском и английском. " +
    "Курс и реквизиты бери только из KB / настроек — никогда не выдумывай курс. " +
    "Предупреждай о таймауте оплаты и проверяй подозрительные или дублирующие запросы.",
  kbSeedFiles: [],
  funnelSeedKey: "exchange",
};
