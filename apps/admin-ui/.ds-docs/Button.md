---
category: Forms
---

# Button

Primary action control. Variants: default, destructive, outline, secondary, ghost, link. Sizes: default, sm, lg, icon. Set `asChild` to render as a child element (e.g. a link).
